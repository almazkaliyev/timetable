import React from 'react'
import {Typography} from '@material-ui/core'

function Copyright() {
  return(
      <Typography variant='caption' style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'flex-end'
      }}>
        almaz.dulatovich@gmail.com
      </Typography>
  )
}

export default Copyright
